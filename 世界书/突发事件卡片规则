<突发事件卡片规则>
突发事件卡片:
  task: 正文<now_plot>末尾用<突发事件>标签给出3~5条互斥的行动分支。只给选择，不预告任何一条的结果，也不代替玩家决定。

  时机:
    出卡: 抵达新场所且有可互动对象或情境钩子；场景质变（只剩两人、天色转暗、对象状态明显变化）；数值刚跨过门槛解锁了新可能
    不出卡: 玩家已明确说了下一步；同一情境上轮出过且无实质变化；对话往返或一段动作的中途；玩家正在看直播（那时用<直播间>）
    一轮最多一张

  字段:
    必写:
      标题: 一句话概括场面，8~16字
      简述: 单行，40~80字，只写环境与对象当下状态，不写玩家心理，不预告结果
      选项: 后接3~5行，每行以「- 」开头
    选写（默认省略，HUD自动读状态栏；只在事件不在当前位置时才覆写）:
      区域: 「大区·地点」，地点名照抄状态栏或地图节点，别自己编，编了底图会退化成城市总览
      场所: 地点内部的具体位置，如「观景崖边」
      对象: 同行角色名，独行填「无」
      时段 / 私密度: 时段用状态栏词；私密度是0~5整数

  硬约束:
    - 顶层字段每行一个，简述禁止换行——折行的部分会被静默丢弃
    - 自由文本里不能出现「字段名+冒号」，会被切成新字段
    - 选项行内字段顺序固定：分类 → 门槛（可省）→ 选项 → 结算
    - 标签放在正文段落之后，不进代码块或其他HTML标签
    - 拿不准就省略。省略有兜底，写错会被丢且不报错

  选项:
    分类: 逐字取自 [纯爱、日常、生理窘迫、调教、睡奸、催眠奸、特殊H]，决定配色
    选项: 玩家要做的动作，祈使句，12~30字。不要在这里写结果
    门槛: 可省。不满足则该选项灰掉并隐藏结算
    结算: 该分支的变化预告
    配比:
      - 至少一条无门槛，否则数值不够时整卡锁死
      - 按强度从低到高排，卡片色调取自最后一条未锁定选项的分类
      - 选项之间必须互斥，不能是同一件事换措辞
      - 带门槛的不超过3条，数值参照对象当前值只略高略低

  门槛（只认三种，其余写法一律视为无门槛直接开放）:
    数值: 变量仅限 好感度/顺从度/性欲度/体力/对象体力/尿意，运算符仅限 ≥ ≤ > <，右侧纯数字
    状态: 异常状态含: 关键词
    道具: 需携带: 物品名
    多条件用「 · 」连接，全满足才解锁
    反例: 「需要她足够顺从」「信任度 ≥ 50」都等于没门槛

  结算:
    - 多项用「, 」分隔，三种写法：键+数字 / 键-数字、键→文字、键重置（或归零）
    - 有专属图标的键: 好感度 顺从度 性欲度 体力 尿意 异常状态 心情 金钱 近期性经验次数；以「经验」结尾的键自动套金星
    - 一条控制在2~4项
    - !!!重要!!! 结算只是给玩家看的预告，不会真的改变量。真实数值变化在玩家选择后由状态栏更新指令执行，两处数字必须一致

  format: |-
    <突发事件>
    标题: ${场面}
    简述: ${单行描写}
    选项:
    - 分类: ${分类} 选项: ${动作} 结算: ${键+数字, 键→文字}
    - 分类: ${分类} 门槛: ${可判定条件} 选项: ${动作} 结算: ${键+数字}
    </突发事件>

  示例（事件就在当前位置，省略全部选写字段）:
    <突发事件>
    标题: 收工后的空录音棚
    简述: 调音台推子还停在她最后一次试唱的位置，隔音门一关走廊的声音全断了，她把耳返摘下来挂在麦架上，额角还沾着汗。
    选项:
    - 分类: 日常 选项: 把水递过去，让她先坐下缓一缓 结算: 好感度+6, 体力+5
    - 分类: 纯爱 选项: 说你刚才在监听里听哭了，想再听一遍那句 结算: 好感度+15, 心情→开朗
    - 分类: 生理窘迫 门槛: 尿意 ≥ 60 选项: 装作没听懂她的暗示，继续把话题拖下去 结算: 尿意+15, 心情→焦躁
    - 分类: 调教 门槛: 顺从度 ≥ 350 选项: 让她重新戴上耳返，按你在监听里说的做 结算: 顺从度+20, 羞耻经验+1, 心情→害羞
    </突发事件>

  高频错误:
    - 简述折行 → 第二行起全丢
    - 简述里写「她嘟囔了句『结算: 明天再说』」 → 简述被截断
    - 门槛写自然语言或用表外变量 → 等于无门槛，直接解锁
    - 编造分类名 → 失去配色
    - 全部选项都带门槛 → 数值不够时整卡死锁
    - 选项里预告结果（「吻她，她会红着脸接受」）→ 结果该由玩家选完再演绎

  大区名: [乌溪区、明湖区、鼓岭区、西洲区、落霞区、雨石与浦江区、青屏山风景区、东塘区]
</突发事件卡片规则>
